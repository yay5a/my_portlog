diff --git a/content/reports/caddy-reverse-proxy-ddns-network.mdx b/content/reports/caddy-reverse-proxy-ddns-network.mdx
index 0d4674f..2dd56be 100644
--- a/content/reports/caddy-reverse-proxy-ddns-network.mdx
+++ b/content/reports/caddy-reverse-proxy-ddns-network.mdx
@@ -9,34 +9,67 @@ This continues **Part 1** (SSH + DHCP reservation). Caddy reverse proxy integrat
 
 ---
 
+## Acknowledgements of Open-Source Projects
+
+#### deSEC
+
+a free DNS hosting service, designed with security in mind. Running on open-source software and supported by SSE, deSEC is free for everyone to use.
+
+![deSEC](/deSEC-dedyn.io.png)
+> [deSEC](https://desec.io/)
+
+> Github repository: [github.com/desec-io/desec-stack](https://github.com/desec-io/desec-stack)
+
+#### Caddy 
+
+one of the few web servers with out-of-the-box support for HTTP/3 and QUIC, which are the latest advancements in web protocols. This feature ensures faster load times and better performance, especially for mobile users or clients with unstable network connections
+
+![Caddy](/caddy.png)
+> [Caddy Server](https://caddyserver.com/)
+
+> Github repository: [github.com/caddyserver/caddy](https://github.com/caddyserver/caddy)
+
+#### Immich 
+
+an open-source self-hosted photo and video management platform built with modern web technologies.  
+It provides automatic backup from mobile devices, AI-powered search and face recognition, and a polished web interface.
+
+![Immich](/immich-app.png)
+
+> [Immich Project](https://immich.app/)
+
+> GitHub repository: [github.com/immich-app/immich](https://github.com/immich-app/immich)
+
+---
+> Note: CGNAT (Carrier-Grade NAT) means your ISP shares one IPv4 among many customers, so port forwarding to your server is impossible.
 ## TL;DR
 
-* **Working:** Immich is publicly reachable at **`<FQDN>`** over **IPv6 (HTTPS)** via **Caddy**.
-* **Why:** ISP places the router behind **CGNAT** on IPv4, so inbound IPv4 is impossible. We removed the **A** record and published **AAAA‑only**.
+* **Working:** Immich is publicly reachable **ONLY** over **IPv6 (HTTPS)**; reverse-proxied via **Caddy**.
+* **Why:** Router is behind a **CGNAT**. Despite `ip a` showing "global" IPv4 addresses, they are only *global* **locally** on **LAN** and unreachable from the internet.
+* **How:** IPv6 eliminates necessity for NAT by giving every device on a network a unique address which is globally routable (as long as IPv6 firewall is configured properly).
 * **DNS:** deSEC hosts the zone. **NS** records remain at deSEC.
 * **TLS:** Caddy obtained and serves a valid Let’s Encrypt certificate over IPv6.
 * **Verified:** Access confirmed from a cellular network; redirects, cookies, and WebSockets behave correctly.
-* **Caveat:** IPv4‑only networks cannot reach the site. Fallback: **Cloudflare Tunnel** or obtain a **public IPv4** from the ISP.
+* **Caveat:** Clients on **IPv4‑only networks** cannot reach the site. Fallback: **Cloudflare Tunnel/Tailscale Funnel** or obtain a **public IPv4** from the ISP.
 
 ---
 
-## [Immich-app](https://github.com/immich-app/immich)
-![Image of Immich from its github repository](/immich-app.png)
+## The Initial Plan
 
----
+1. **IPv6‑only public access:**
+
+   Configured server and IPv6 firewall for inbound internet traffic.
+
+2. **Stopped configuring IPv4:**
 
-## What changed vs. the initial plan
+   IPv4 port‑forwarding cannot work through CGNAT.
 
-1. **IPv6‑only public access.**
-   Deleted the **A** record; kept **AAAA** pointed at `<GLOBAL_IPV6>`.
+3. **Caddy is the HTTPS entrypoint:**
 
-2. **Stopped pursuing IPv4 port‑forwarding.**
-   Router WAN shows `100.x.x.x` (CGNAT). Port‑forwarding cannot work through CGNAT.
+   Terminates TLS and reverse‑proxies to Immich; auto‑renews certs over IPv6 via *ACME (Automated Certificate Management Environment)*.
 
-3. **Caddy is the HTTPS entrypoint.**
-   Terminates TLS and reverse‑proxies to Immich; auto‑renews certs over IPv6.
+4. **DDNS simplified:**
 
-4. **DDNS simplified.**
    deSEC manages the name; updates target **IPv6** only. (Add IPv4 later if public address is acquired, or front with a tunnel.)
 
 ---
@@ -59,26 +92,23 @@ Ubuntu host
 
 ## deSEC (DNS)
 
-* **Records**
+**Records:**
 
   * `AAAA  <FQDN>  <GLOBAL_IPV6>`
   * `NS    <domain>  ns1.desec.io / ns2.desec.org` (unchanged)
-* **Deliberately absent:** `A` (IPv4) — removed to avoid broken dual‑stack behavior.
-
-> Placeholders are sanitized: `<FQDN>`, `<GLOBAL_IPV6>`.
 
 ---
 
 ## Docker & Caddy (sanitized)
 
 ### Project layout
-
+> ddns-updater has been removed from the stack, but kept for information purposes; explained below.
 ```
 /opt/immich/
 ├─ docker-compose.yml
 ├─ Caddyfile
 ├─ .env
-└─ ddns-updater/
+└─ddns-updater/
    └─ config.json
 ```
 
@@ -99,6 +129,7 @@ services:
     image: caddy:latest
     container_name: caddy
     depends_on: [immich-server]
+    network_mode: host # This was removed due to docker network conflict; explained below
     ports:
       - "80:80"
       - "443:443"
@@ -106,20 +137,30 @@ services:
       - ./Caddyfile:/etc/caddy/Caddyfile:ro
       - caddy_data:/data
       - caddy_config:/config
-    restart: unless-stopped
+    restart: always
 
+# ddns-updater has been removed from the compose stack (explained below)
   ddns-updater:
     image: qmcgaw/ddns-updater
     container_name: ddns-updater
     volumes:
-      - ./ddns-updater:/updater/data
+     - ./ddns-updater:/updater/data
     environment:
-      - PERIOD=5m
+     - PERIOD=5m
     restart: unless-stopped
 
 volumes:
+  model-cache:
   caddy_data:
   caddy_config:
+  caddy_logs:
+
+networks:
+  default:
+    driver: bridge
+    enable_ipv6: true
+    ipam:
+      driver: default
 ```
 
 ### `Caddyfile` (sanitized)
@@ -144,8 +185,9 @@ volumes:
 }
 ```
 
-### DDNS (`ddns-updater` config; sanitized)
+### ~~DDNS (`ddns-updater` config; sanitized)~~
 
+> The following has been removed from the container (explained below)
 ```json
 {
   "settings": [
@@ -153,25 +195,28 @@ volumes:
       "provider": "desec",
       "domain": "<FQDN>",
       "token": "<DESEC_TOKEN>",
-      "ip_version": "ipv6"
-    }
+     "ip_version": "ipv6"
+   }
   ]
 }
 ```
 
-> Add an IPv4 block later only if you obtain a true public IPv4 or terminate a tunnel on a public endpoint.
+> ~~Add an IPv4 block later only if you obtain a true public IPv4 or terminate a tunnel on a public endpoint.~~
 
 ---
 
-## Router & host firewall
+## Router & host (server) firewall
+
+**Router:**
 
-* **Router:** IPv6 **On**; inbound **80/443 TCP** allowed to the server. WAN IPv4 is `100.x.x.x/xx` (CGNAT).
-* **Server firewall (UFW):**
+IPv6 inbound **80/443 TCP** allowed to the server.
 
-  ```bash
-  sudo ufw allow 80/tcp
-  sudo ufw allow 443/tcp
-  ```
+**Server firewall (UFW):**
+    ```bash
+    sudo ufw allow 80/tcp
+    sudo ufw allow 443/tcp
+    ```
+> No inbound ports besides 80/443 are open. SSH access is restricted to my LAN and rate-limited via UFW.
 
 ---
 
@@ -212,9 +257,10 @@ echo | openssl s_client -connect <FQDN>:443 -servername <FQDN> 2>/dev/null \
 
 ## Why IPv6‑only is the right fit
 
-* Router’s WAN IPv4 is CGNAT (`100.64.0.0/10`) → inbound IPv4 cannot reach the LAN.
+* Router’s WAN IPv4 is CGNAT (`100.x.x.x`) → inbound IPv4 cannot reach the LAN.
 * ISP provides **global IPv6**; inbound 80/443 over IPv6 works.
 * Removing the A record prevents clients from attempting an unreachable IPv4 path first.
+* Most IPv4-only clients attempt A records before AAAA records, so removing the A record avoids failed connection attempts.
 
 **Limitation:** users on **IPv4‑only** networks cannot connect.
 
@@ -228,26 +274,52 @@ echo | openssl s_client -connect <FQDN>:443 -servername <FQDN> 2>/dev/null \
 
 ---
 
-## Sanitized runbook
+## Misconfigurations discovered and resolved
 
-**Daily**
+During deployment I hit a couple of snags that prevented the stack from working at first.
 
-```bash
-# Caddy renewals / errors
-journalctl -u caddy --since "yesterday"
+* **DDNS updater misadventure.** 
 
-# Quick reachability
-curl -6I https://<FQDN>
-```
+Attempted to automate DNS updates using the `ddns‑updater` container with a configuration file that pushed my AAAA record to deSEC. This caused the service to crash and, even when removed, did not resolve my connectivity issues. In hindsight the updater was a red herring—my real problem was the Docker network configuration described below. I’ve decided not to reintroduce `ddns‑updater` or write my own updater script. If the server crashes again, troubleshooting by checking the logs for any IP related issues will narrow down the culprit easily.
+
+
+* **Docker network mismatch.** 
+
+Set `network_mode: host` on the Caddy service while the other containers were on the default bridge network. This prevented Caddy from reaching `immich-server` by its service name and resulted in connection errors. Docker’s bridge network isolates containers behind a virtual switch, while host mode shares the server’s own network stack. Mixing the two prevented name-based resolution between containers. The solution was to remove the `network_mode` line and let Compose manage the network. Caddy and Immich now share the same default bridge network (optionally with `enable_ipv6: true`), so `reverse_proxy http://immich-server:2283` works as expected.
+
+> All other services remain internal to the Docker network and are not directly exposed.
+
+--- 
+
+## Self‑hosting Immich: options considered and my choice
+
+Through this project I learned there are several ways to self‑host Immich:
+
+1. **Local network only.** Expose Immich directly on the LAN by mapping port 2283 (e.g. `ports: "2283:2283"`) and access it via IP. This works for purely internal use but leaves uploads unencrypted and isn’t reachable over the internet.
+- Pros: Simple setup, no reverse proxy needed.
+- Cons: Unencrypted traffic; not accessible outside the LAN.
+
+2. **Caddy reverse proxy (my choice).** Run a Caddy container in the compose stack, bind ports 80 and 443, and proxy requests to `immich-server`. Caddy automatically obtains and renews TLS certificates and lets me add headers and compression easily.
+- Pros: Automatic HTTPS, minimal config, secure.
+- Cons: Requires open ports 80/443.
+
+3. **Traefik reverse proxy.** Similar to Caddy but uses labels and dynamic routing. Traefik is powerful in large multi‑service deployments but adds complexity for a single app.
+- Pros: Great for scaling and multi-service orchestration.
+- Cons: Higher learning curve; overkill for one service.
 
-**DNS toggle**
+4. **VPN‑only access (e.g. Tailscale).** Don’t expose any ports; instead join all family devices to a mesh VPN and access Immich via its private IP. This is secure but requires every user to install and sign in to the VPN.
+- Pros: Very secure; no public exposure.
+- Cons: Requires VPN client and login for every user.
 
-* Keep **AAAA** updated.
-* Add **A** only if a public IPv4 becomes available (or a tunnel is added).
+5. **Cloud tunnels (e.g. Cloudflare Tunnel).** Use a tunnel agent to publish Immich through a cloud proxy. This bypasses NAT/firewall issues but adds an external dependency and potential costs.
+- Pros: Works even behind CGNAT; zero-config HTTPS.
+- Cons: External dependency; potential vendor lock-in or cost.
 
-**Backups**
+6. **Public IPv4 with direct forwarding.** If your ISP provides a real public IPv4, you can use an `A` record and forward ports 80/443 through your router.
+- Pros: Traditional and straightforward.
+- Cons: Relies on rare or paid IPv4 availability; manual cert management if not proxied.
 
-* Snapshot `Caddyfile`, `docker-compose.yml`, `.env`, and Immich storage volumes.
+Because my ISP uses carrier‑grade NAT and only provides global IPv6, and getting my family to download and configure Tailscale for VPN-only access seemed convoluted, I chose **option 2**; using Caddy behind deSEC with an AAAA record allowing secure HTTPS access without needing a public IPv4 address. Caddy’s simplicity (single file configuration, automatic TLS) made it easy to manage, and the IPv6‑only deployment meets my family’s needs. If IPv4 connectivity becomes necessary I can layer a Cloudflare Tunnel/VPN through Tailscale, or acquire a public IPv4 without re‑architecting the stack.
 
 ---
 
@@ -264,6 +336,7 @@ UPLOAD_LOCATION=/srv/immich/library
 ```
 
 ## Appendix B — deSEC one‑off update (sanitized)
+> I removed the routine updater from running automatically, if the server can no longer be reached due to IP misconfiguration, running this script *should* resolve it.
 
 ```bash
 # Seed/update AAAA manually; routine updates handled by ddns-updater
@@ -282,4 +355,4 @@ curl "https://update.dedyn.io/?hostname=<FQDN>&myipv6=$(curl -s -6 ifconfig.co)"
 
 > All sensitive details are sanitized: `<FQDN>`, `<GLOBAL_IPV6>`, `<ACME_EMAIL>`, `<DESEC_TOKEN>`, `<REDACTED>`.
 
-> *Documentation drafted with AI assistance (ChatGPT). All procedures performed, tested, and validated manually on a live Ubuntu Server.*
+> *This report was templated with AI assistance (ChatGPT) and manually verified by me. All procedures were performed, tested and validated on a live Ubuntu Server.*
diff --git a/public/immich-app.png b/public/immich-app.png
index d860175..003c3cf 100644
Binary files a/public/immich-app.png and b/public/immich-app.png differ
